pipeline {
    agent any

    stages {
        stage('Initialize') {
            steps {
                cleanWs()
            }
        }
        stage('Get SCM') {
            steps {
                git "https://github.com/yanivomc/docker-cicd.git"
            }
        }
        stage('docker build'){
            steps {
                docker build -t saleh-app . 
                docker images
            }
        }
        stage('docker Deploy'){
            steps {
                docker run -itd saleh-app &  
            }
        }
    }   
}

